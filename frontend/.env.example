# Docker Compose dev: navegador acessa a API na maquina (porta publicada).
VITE_API_URL=http://localhost:3000

# Kubernetes / producao (Dockerfile.prod): deixe vazio ou omita —
# use o arquivo .env.production (URLs relativas / mesmo origin).
